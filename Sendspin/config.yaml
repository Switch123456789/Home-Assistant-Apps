slug: "sendspin"
name: "Sendspin"
description: "Sendspin client for local playback"
version: "0.20"
init: false
audio: true
realtime: true
host_network: true

arch:
  - aarch64
  - amd64
  - armhf
  - armv7
  - i386

startup: services

schema:
  bool_boost_volume: bool?
  str_name: str?
  str_id: str?
  str_url: str?
  str_log_level: list(DEBUG|INFO|WARNING|ERROR|CRITICAL)?